import React from 'react';

const Dashboard = (props) => (
    <div className="bg-primary container-fluide p-4">
        <section className="jumbotron">
            <h2>Contacts Book <small>a simple react crud app</small></h2>
        </section>
    </div>
);

export default Dashboard;